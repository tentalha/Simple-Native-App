import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    listText: {
        fontSize: 24,
        textAlign: "center",
        marginVertical: 20
    },
    listContainer: {
        flex: 8
    },
    listItem: {
        fontSize: 20,
        marginHorizontal: 10
    }
})